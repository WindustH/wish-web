# 开始使用

[文档目录](README.md)

按[部署说明](deployment.md)启动 wish 和 WebUI。在设置中配置提供方和默认模型，密钥填写后端环境变量名。首页输入消息后会创建会话并开始执行。

会话管理支持重命名、标签、复制、手动压缩、清空当前上下文和删除。修改模型配置或取消排队消息前，先中断并等待收尾。清空上下文保留历史记录；压缩之后也能通过历史搜索回顾内容。

图片作为协议图片块输入。文件附件通过服务器本地路径交给 shell 工具访问。统计展示实际记录到的用量；上游未提供的指标不会被推算成已知值。
